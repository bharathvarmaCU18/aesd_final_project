git clone git clone git@github.com:bharathvarmaCU18/aesd_final_project.git
git submodule update --init --recursive
run the build.sh
Once the image is ready, extract core-image-sato-dev-jetson-nano-devkit.tegraflash.tar and flash the image onto the jetson nano.
